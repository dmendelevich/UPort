# Backlog UPort

Список отложенных задач и технического долга. Разложено по трекам — см. [06_tracks.md](06_tracks.md). Приоритет обсуждается с пользователем отдельно.

## Открыто — Трек A (Советник)

1. **Написать триггер избыточного кэша + расчёт лесенки входа.** Сравнение факта `Кэш/Резерв` (через `PortfolioInspector.get_virtual_cash_balances`) с планом → при превышении порога запуск поиска кандидатов (уже есть SQL-скринеры по стратегиям в `anal_core.md`/`rules_config`) → расчёт первой ступени лесенки (сумма перевода со накопительного на торговый счёт, цена лимитного приказа) по правилам `strategy_tactics` конкретной стратегии. *(зафиксировано 2026-07-22)*

2. **Собрать дайджест v0 (прототип, не обязательно через Telegram).** Прогон по текущему портфелю: вход-оценщик (уже есть) + оценщик выхода (см. «Сделано» п.7) + триггер кэша (п.1) + аудит лимитов (`PortfolioInspector.audit_limits_and_rules`, уже есть) → читаемый текстовый отчёт. Цель — проверить на живых данных П10/П136, что советы разумны, до встраивания в бота. **Дайджест только советует** — никаких автоматических ордеров/переводов, только текстовые инструкции для ручного исполнения в терминале брокера (принцип согласован 2026-07-22, см. [06_tracks.md](06_tracks.md)). *(зафиксировано 2026-07-22)*

3. **Развести утренний батч-дайджест и срочные событийные уведомления.** Сценарий "актив резко падает — продавай срочно" — это не раз-в-утро дайджест, а отдельный триггер поверх уже существующего 5-минутного цикла синхронизации алертов. Нужно решить, как эти два канала (утренний Хрон vs событийный) делят общую оценочную логику из пп. 1 и «Сделано» п.7. *(зафиксировано 2026-07-22)*

4. **Открытый вопрос: что делать с Револьверной позицией на грани 30-дневного тайм-аута, если импульс ещё жив?** Сейчас `PositionExitEvaluator` жёстко ликвидирует слот по истечении 30 дней, если цель `+15%` не достигнута — независимо от текущего RSI/тренда. Пользователь привёл пример: бумага 29 дней уверенно растёт, анализ показывает потенциал продолжения роста — жаль принудительно продавать по одному календарному правилу. Возможная идея (не решена): вместо жёсткой ликвидации в этом краевом случае — предложить перевод в Трендовую стратегию (перенос между стратегиями технически уже реализован, см. `bot_keyboards.generate_target_strategies_keyboard` + `bot_handlers/strategies.py` — универсальный, не завязан на конкретную пару стратегий). Требует отдельного обсуждения алгоритма. *(зафиксировано 2026-07-22, со слов пользователя)*

5. **Открытый вопрос: нужен ли общий рыночный "предохранитель" на случай обвала рынка?** Пользователь отметил, что при резком обвале всего рынка точечные правила выхода по каждой стратегии могут быть недостаточны — нужен какой-то надпортфельный защитный механизм. Пользователь сам не знает, как это реализовать; идея зафиксирована для будущего обсуждения, решение не принято. *(зафиксировано 2026-07-22, со слов пользователя)*

## Открыто — Трек B (Административный интерфейс стратегий)

6. **Разработать интерфейс заведения новых стратегий из шаблонов** (для админа/пользователя) — добавление стратегии в существующий портфель на основе записи из `strategy_templates`, с копированием `rules_config`/`human_philosophy` в новую строку `strategies` (так же, как при создании портфеля). *(зафиксировано 2026-07-21)*

7. **Разработать интерфейс перераспределения долей (`strategy_share_pct`) между стратегиями портфеля.** Сейчас доли выставляются вручную/при создании и нигде не проверяются на сумму (например, у П10: 90%+10%=100%, у П136: 65%+30%+5%+0%=100% — совпадает случайно/вручную). Нужен экран, где можно менять доли нескольких стратегий портфеля согласованно. *(зафиксировано 2026-07-21)*

8. **Подключить портфели сына.** Portfolios у Freedom Broker и Trading212 для owner_id=4 (МДМ) ещё не созданы/не настроены. *(зафиксировано 2026-07-20)*

## Открыто — Трек C (Наведение порядка)

9. **Актуализировать архитектурное описание системы.** `ARCHITECTURE/ARCHITECTURE_tickers.md` (v10.2) описывает только справочный слой (`tickers`/`exchanges`/паспортизация/сигналы/фундаментал) и не отражает слой `brokers` → `listings` → `watchlist` → `portfolios`, введённый рефакторингом v3.0 (коммит `d2bc335`). Нужно новое полное описание архитектуры БД и потоков данных. *(зафиксировано 2026-07-20)*

10. **Удалить портфель 9999 "Семейная лаборатория ИИ".** Устаревшая концепция — её роль (отслеживание общего интереса к бумаге) теперь выполняет `provenance` в `tickers`. Пока НЕ удалять — есть риск, что что-то на неё завязано; сначала найти и убрать все зависимости. *(зафиксировано 2026-07-20)*

11. **Убрать неиспользуемую фазу `considered_at` в `watchlist`.** Заменена на `provenance` в `tickers`. Нужно решить: перестать писать в колонку / удалить колонку / почистить код, который её ещё использует. *(зафиксировано 2026-07-20)*

12. **Выполнить очистку "чёрного списка" колонок в `tickers`.** Запланирована в `ARCHITECTURE_tickers.md` §1.2, но физически не выполнена: `yahoo_symbol`, `rsi_14`, `fb_market`, `fb_exchange`, `isin`, `last_updated_at`, `max_ticker_div_yield_pct`, `turnover_last_synced_at`. Перед удалением — проверить код на использование каждой колонки. *(зафиксировано 2026-07-20)*

13. **Перевести существующий код на новые универсальные view.** `create_universal_views.py` создал `v_listings_tickers`, `v_assets_full`, `v_watchlist_full`, `v_orders_full`, `v_order_pipelines_full`, `v_strategy_assets_full` (см. [02_universal_views.md](02_universal_views.md)), но ~15+ файлов кода всё ещё используют ручные JOIN напрямую. Нужно отдельное обсуждение, какие файлы и в каком порядке переводить. *(зафиксировано 2026-07-20)*

14. **Учесть при переводе `watchlist.py` на `v_watchlist_full`:** для портфеля 9999 старый код считал активные алерты без фильтра по `portfolio_id` (по всем портфелям на листинге), новый view фильтрует по `portfolio_id`. Сейчас не расходится (у 9999 нет записей в вотчлисте), но при миграции кода — проверить. Актуально только пока портфель 9999 не удалён (см. п.9). *(зафиксировано 2026-07-20)*

15. **Перевести весь код на единый паттерн доступа к БД.** `database.py` предоставляет `execute_query()` (список строк) и `execute_row()` (одна строка словарём, поверх `execute_query`, для случаев "нужна ровно одна запись"). Пока только 5 файлов используют `execute_row()` последовательно (`bot_handlers/summary.py`, `bot_handlers/tickers.py`, `brokers_connectors/sync_strategy_asset_fb.py`, `database.py` частично, `test_evaluator_v2.py`); 22 файла всё ещё делают ручные проверки `isinstance(res, list)` вместо `execute_row()` — больше всего в `database.py` (20 мест), `utils.py` (13), `bot_handlers/watchlist.py` (8), `analytics/portfolio_auditor.py` (7). Переводить постепенно, по мере доработки процессов — не отдельным большим рефакторингом. *(зафиксировано 2026-07-20)*

16. **Урезать таблицу `portfolios` до `id`, `name`, `owner_id`, `broker_id`.** Остальные поля (`strategy_type`, `risk_profile`, `min_cash_reserve_pct`, `max_ticker_weight_pct`, `target_return_pct`, `max_portfolio_div_yield_pct`) признаны пережитками старой архитектуры (их роль перешла на уровень отдельных стратегий портфеля — `strategies`/`strategy_templates`). Перед удалением нужно поправить ссылки на них в коде: `strategy_type` используется в `analytics/portfolio_auditor.py` и `bot_handlers/watchlist.py`; `risk_profile`, `min_cash_reserve_pct`, `max_ticker_weight_pct`, `target_return_pct`, `max_portfolio_div_yield_pct` — только в `analytics/portfolio_auditor.py`; также вошли в `v_assets_full`/`v_order_pipelines_full` (`create_universal_views.py`) — при удалении колонок надо будет пересоздать эти view. *(зафиксировано 2026-07-21)*

17. **`BEGIN`/`ROLLBACK` отдельными вызовами `execute_query()` не дают реальной транзакции.** `uport_ai_gateway.py` открывает новое `psycopg2.connect()` на каждый HTTP-запрос — поэтому `db_sys.execute_query("BEGIN;")` в одном вызове и `db_sys.execute_query("ROLLBACK;")` в другом работают в РАЗНЫХ соединениях и не связаны друг с другом; всё, что между ними, коммитится по факту, "откат" — иллюзия. Обнаружено на практике: `test_strategy_sync.py` оставил в БД реальные тестовые строки (`strategies.id=999`, `assets`, `strategy_assets`) несмотря на финальный `ROLLBACK` — вручную вычищено 2026-07-21. Используется (ошибочно) в `test_strategy_sync.py` и `test_bot_utils.py`; в боевом коде такого паттерна нет (`bot_utils.py:execute_virtual_transfer` шлёт `BEGIN;...;COMMIT;` ОДНОЙ строкой в ОДНОМ вызове — это безопасно). Нужно почистить оба теста (переписать на единый CTE/строку, как в `execute_virtual_transfer`, либо явно предупреждать, что откат не работает). *(зафиксировано 2026-07-21)*

18. **Переделать существующие экраны (`portfolios.py`, `tickers.py`) под стандарт header/body/footer.** Сейчас только карточка стратегии (см. «Сделано» п.5) собрана из общих кубиков (`bot_screens.py`/`bot_keyboards.py`). `portfolios.py:process_view_portfolio` и `tickers.py:process_view_ticker` по-прежнему строят шапку и подвал вручную инлайн, с дублированием между `sub_view`. Перевести на `format_portfolio_header`/переиспользуемые футер-сборщики по образцу стратегии. *(зафиксировано 2026-07-21)*

19. **`build_smart_badge` определена дважды в `bot_handlers/bot_keyboards.py`** (строки 15 и 127) — вторая версия молча перекрывает первую при импорте модуля (отличается обработкой `value==0`: у первой добавляет `SIX_PER_EM`, у второй два `BRAILLE_EMPTY`). Нужно оставить одну версию, проверить, какая реально нужна вызывающему коду. *(зафиксировано 2026-07-21)*

20. **`order_pipelines` заполнена только для одной позиции (OPEN) из-за истории появления концепции, не из-за бага синхронизации.** Обнаружено при написании `PositionExitEvaluator` (Трек A, «Сделано» п.7): из 4 держимых Револьверных позиций портфеля 1 (GLDM, OPEN, PG, UBER) запись в `order_pipelines` есть только у OPEN. Объяснение пользователя: таблицы `strategies`/`strategy_tactics`/`order_pipelines` появились позже остальной системы; `OPEN` — тестовая покупка (самая дешёвая ликвидная бумага), на которой отлаживалось взаимодействие терминала брокера с конвейером, поэтому у неё данные корректны. GLDM/PG/UBER были куплены ещё до начала работ над системой и попали в неё "как есть", без создания конвейера задним числом; у `OPEN` `initial_entry_price = 0.0` — вероятно, тоже артефакт отладки, а не готовое к использованию значение. Не блокирует работу — оценщик выхода использует `assets.avg_price` как источник цены входа, а не `order_pipelines.initial_entry_price`. Стоит отдельно решить: нужно ли (и как) ретроактивно заполнить `order_pipelines` для старых позиций, или обнулить/пересобрать таблицу и вести её только для сделок, совершённых через систему начиная с определённой даты. *(зафиксировано 2026-07-22, со слов пользователя)*

## Сделано
1. **Универсальные view от tickers до состава стратегий** — создано 6 слоистых view (`v_listings_tickers` → `v_assets_full`/`v_watchlist_full`/`v_orders_full`/`v_order_pipelines_full` → `v_strategy_assets_full`), протестировано (без коллизий колонок, без fan-out, сверка с ручными запросами). См. [02_universal_views.md](02_universal_views.md). *(2026-07-20, Трек C)*

2. **Связь `strategies.template_id → strategy_templates.id` + `system_key`.** У словаря `strategy_templates` появился обязательный уникальный смысловой ключ (`system_key`: `REVOLVER`, `CONSERVATIVE_ACCUMULATION`, `TREND_FOLLOWING`, `CASH_RESERVE`, `UNALLOCATED`). У `strategies` появилась обязательная (`NOT NULL`) ссылка `template_id`, все 7 строк проставлены (включая новую — недостающую буферную стратегию портфеля 1). Починен баг поиска буферной стратегии в `_get_unallocated_strategy_id` (`brokers_connectors/sync_strategy_asset_fb.py`) — раньше терял фильтр по `portfolio_id` из-за AND/OR по имени, теперь ищет через `JOIN` по `template_id`/`system_key`. Скрипт миграции — [`fix_strategy_template_links.py`](../fix_strategy_template_links.py). Протестировано: `test_strategy_sync.py` (поправлен под `NOT NULL`), портфель 1 → буфер id=7, портфель 2 → буфер id=6 (раньше оба указывали бы на 6), несуществующий портфель корректно бросает исключение, `v_strategy_assets_full` не пострадал. См. [03_strategies_and_templates.md](03_strategies_and_templates.md). *(2026-07-21, Трек C)*

3. **Конфликт роутеров: глобальный перехватчик текста ломал ввод имени портфеля.** `bot_handlers/ticker_search.py` содержит `@router.message(F.text)` — ловит вообще любой текст без ограничения по FSM-состоянию, зарегистрирован в `uport_ai_bot.py` РАНЬШЕ `portfolio_admin`. Обнаружено пользователем вживую: ввод имени портфеля «ПМ» на Шаге 3 перехватывался ticker_search («Тикер должен содержать только латинские буквы...») вместо `portfolio_admin.process_name_text`. `backlog.py` со схожим FSM-вводом текста работал только благодаря более раннему порядку регистрации своего роутера — везение, а не защита. Системный фикс: `@router.message(StateFilter(None), F.text)` в `ticker_search.py` — перехватчик срабатывает только вне любого активного FSM-состояния, независимо от порядка роутеров. Протестировано напрямую через `HandlerObject.check()` на трёх сценариях (idle-чат, состояние создания портфеля, состояние бэклога) — конфликт исчез, регрессии для бэклога нет. *(2026-07-21, Трек C)*

4. **Служебный модуль создания портфеля в Telegram** (`bot_handlers/portfolio_admin.py`) + фикс шлюза `uport_ai_gateway.py`. Доступен только `is_admin=true`, новая кнопка «➕ Создать портфель» в `settings_main`. Экраны: выбор владельца → выбор брокера → ввод имени (FSM) → подтверждение (`generate_confirm_screen`/`generate_confirm_keyboard`) → атомарное создание портфеля + 2 служебных стратегий (Кэш/Резерв 10%, Неопределенная стратегия 90%) одним CTE-запросом (`WITH ... INSERT ... RETURNING`). Кнопки везде передают числовые ID (добавлены поля `user_id`/`broker_id` в `MenuAction`, `bot_handlers/common.py`). По пути найден и починен баг шлюза: `uport_ai_gateway.py` решал, вызывать ли `fetchall()`, по тому, начинается ли текст запроса со слова `select` — из-за этого `WITH...SELECT` и `INSERT...RETURNING` теряли возвращаемые строки (при этом реально коммитясь!). Заменено на проверку `cur.description is not None` (не зависит от текста запроса). RBAC-предохранитель ролей (AI/BOT/SYSTEM) не тронут — это отдельная, всё ещё нужная защита. `uport_gateway.service` перезапущен. Протестировано: полный сценарий (4 экрана + ветка отмены) мок-интеграционным прогоном с реальным FSM, с реальной записью в БД и последующей уборкой; отдельно проверено сохранение `is_admin`/`user_db_id` в FSM на каждом шаге (по образцу `process_back_to_menu`). **Модуль ещё не подхвачен живым ботом** — `uport.service` нужно перезапустить (`restart_uport.sh`), чтобы кнопка появилась. *(2026-07-21, Трек B)*

5. **Экран «Стратегии» в Telegram.** Новая вкладка «🎯 Стратегии» в карточке портфеля (`portfolios.py`, доступна только для реальных персональных портфелей, не для сводного/9999) → карточка конкретной стратегии (`bot_handlers/strategies.py`, новый экран): план/факт капитала и риск-аудит через уже существующий `PortfolioInspector`, список бумаг внутри стратегии через `v_strategy_assets_full`, клик по бумаге ведёт в уже существующую карточку тикера. Первый экран, полностью собранный из общих «кубиков» header/footer (`format_strategy_header` в `bot_screens.py`, `generate_tab_switch_keyboard` в `bot_keyboards.py`) — начало реализации Трека C, п.17. Добавлено поле `strategy_id` в `MenuAction`. Протестировано мок-интеграционным прогоном на реальных данных П10/П136: обе активные стратегии с позициями и без (Кэш/Резерв), переход в карточку тикера, несуществующая стратегия, сводный портфель — без падений, `callback_data` в пределах лимита Telegram (макс. 49 байт из 64). **Ещё не подхвачено живым ботом** — нужен `restart_uport.sh`. *(2026-07-21, Трек B)*

6. **`TickerEvaluator.evaluate_ticker_strategy` переведён с сырых `strategies.id` (1/2/3) на классификацию по `system_key`.** (`analytics/analytics_utils.py`). Раньше блоки "Стратегия 1/2/3" искались по `if 1 in strategy_configs` и т.п. — жёстко предполагая, что Револьверная всегда лежит под id=1, Консервативная под id=2, Трендовая под id=3. Для двух текущих портфелей это случайно совпадало (портфель 1: Револьверная реально id=1; портфель 2: Консервативная id=2, Трендовая id=3), поэтому баг был не виден, но сломался бы на любом новом портфеле (сын, Трек B п.7) или новой стратегии из шаблона (Трек B п.5), где `strategies.id` — сквозной автоинкремент без привязки к порядку. Исправлено: SQL загрузки конфигов теперь джойнит `strategy_templates` и строит `strategy_configs`/`strategy_ids`, ключ — `system_key` (`REVOLVER`/`CONSERVATIVE_ACCUMULATION`/`TREND_FOLLOWING`); `explain_map`/`compatible_strategies` по-прежнему адресуются реальным `strategies.id` (нужен для будущих кнопок бота — записи в `strategy_assets` идут по FK на `strategies.id`, не на `system_key`; проверено на примере уже работающего паттерна в `bot_handlers/strategies.py:19,44-49`, где реальный `strategy_id` используется для адресации, а `system_key` дозапрашивается JOIN'ом только для смысловых веток типа `if system_key == "CASH_RESERVE"`). Устаревшие `test_evaluator.py`/`test_evaluator_v2.py` удалены (не стоили поддержки, не входили в тестовый паттерн проекта). Проверено вручную прогоном на живых данных: портфель 1 → `explain_map` содержит только id=1 (Револьверная); портфель 2 → id=2 и id=3 (Консервативная, Трендовая) — соответствует реальной структуре `strategies` в БД. *(2026-07-22, Трек A)*

7. **Написан `PositionExitEvaluator`** (`analytics/position_exit_evaluator.py`) — сканирует уже открытые позиции портфеля (`assets` + `strategy_assets` + `strategy_templates`) и по `system_key` применяет правило выхода той стратегии, к которой бумага сейчас привязана: Трендовая — слом веера (`current_price < EMA20` ИЛИ `EMA20 < SMA50` ИЛИ `RSI > 80` и `MACD < 0`), Консервативная — фундаментальный разлад (`ROE < 10%` / `debt_to_equity > 2.0` / чистый убыток), Револьверная — цель `+15%` от цены входа (сигнал "продавай", если `RSI ≤ 65`; если `RSI > 65` — сигнал "придержи, рассмотри перевод в Трендовую" вместо продажи, по просьбе пользователя) либо тайм-аут слота 30 дней без достижения цели. `CASH_RESERVE`/`UNALLOCATED` — пропускаются, тактики выхода нет. Только рекомендации (SELL/HOLD с обоснованием), никаких действий с ордерами. Два прикладных нюанса, найденных и исправленных по пути: (1) цена входа для Револьверной берётся из `assets.avg_price`, а не `order_pipelines.initial_entry_price` — на живых данных у 3 из 4 держимых Револьверных позиций (GLDM/PG/UBER) запись `order_pipelines` вообще отсутствует, у четвёртой (OPEN) `initial_entry_price=0.0` (см. Трек C, новый п.20); (2) `NULL` в фундаментальных полях (`return_on_equity`/`debt_to_equity`/`pe_trailing`) трактуется как "неприменимо" (напр. ETF без ROE), а не как "0" — иначе `GLDM` (золотой ETF в портфеле 2) ложно диагностировался бы как "фундаментал сломался". Протестировано прогоном на живых позициях обоих портфелей: портфель 1 — все 4 Револьверные позиции ушли за 30-дневный тайм-аут (мониторинга раньше не было); портфель 2 — 7 из 12 Трендовых позиций дали сигнал слома тренда, `GLDM` (Консервативная) корректно не флагуется после фикса NULL. *(2026-07-22, Трек A)*
