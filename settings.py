# /root/UPort/settings.py
"""
Инвестиционный манифест семьи UPort.
Здесь хранятся бизнес-параметры, сита ликвидности и триггеры технических стратегий.
"""

# ─── СИТО ЛИКВИДНОСТИ (ОТСЕЧЕНИЕ ШУМА) ───
# Минимальный суточный оборот в долларах США. 
# Бумаги с оборотом ниже этого значения утром не отправляются в TradingView.
MIN_DAILY_TURNOVER_USD = 500_000_000

# ─── ПАРАМЕТРЫ СТРАТЕГИИ «ПАДШИЕ АНГЕЛЫ» ───
# Максимальное значение утреннего RSI(14). Всё, что ниже — признается перепроданным.
TV_FALLEN_ANGEL_RSI_MAX = 45.0

# ─── ГЕОГРАФИЯ И ПЛОЩАДКИ ТОРГОВЛИ TRADINGVIEW ───
TV_DEFAULT_SCREENER = "america"
TV_PREFERRED_EXCHANGES = ["NASDAQ", "NYSE"]

# ─── PRICE MOVE WATCHER (индикатор резкого движения цены, Блок 2 карточки тикера) ───
# см. Claude/05_strategy_screen_and_kubiki.md. Окно задаётся в МИНУТАХ, а не в числе
# отсчётов -- устойчиво к изменению частоты синка котировок (QUOTES_SYNC_INTERVAL,
# cron_scheduler.py): расчёт ищет ближайшую по времени котировку не позже "N минут
# назад" по recorded_at, а не N-ю по счёту запись.
PRICE_MOVE_WATCHER_WINDOW_MINUTES = 45      # окно для расчёта % движения цены
PRICE_MOVE_WATCHER_BUFFER_SIZE = 20         # запас отсчётов в кольцевом буфере (не привязан жёстко к окну выше)
PRICE_MOVE_WATCHER_THRESHOLD_PCT = 5.0      # ЗАПАСНОЙ плоский порог -- используется только если у бумаги ещё нет
                                             # tickers.signal_daily_volatility_pct (см. блок ниже), напр. только что
                                             # добавленный тикер без ночной истории
PRICE_MOVE_WATCHER_PERIODIC_SEC = 3600      # повтор уведомления, пока условие не снято (см. alerts.periodic у ФБ)

# ─── ВОЛАТИЛЬНОСТЬ БУМАГИ (общий примитив, см. Claude/BACKLOG.md, 2026-07-30) ───
# tickers.signal_daily_volatility_pct -- std дневных % изменений цены за окно ниже,
# считается раз в сутки в site_connectors/sync_signals_yf.py вместе с остальными
# сигналами (EMA/RSI/MACD), из уже загруженной истории цен -- отдельного запроса не
# требует. Единый фундамент для НЕСКОЛЬКИХ потребителей (PriceMoveWatcher и
# протухание приказов/алертов ниже) -- каждый применяет свой K-множитель к одной и
# той же цифре, а не считает волатильность заново по-своему.
DAILY_VOLATILITY_WINDOW_DAYS = 20                  # окно для std дневных % изменений цены

# PriceMoveWatcher: ожидаемое движение за окно = daily_volatility_pct × √(окно/торговый_день) × K
PRICE_MOVE_WATCHER_VOLATILITY_MULTIPLIER = 2.0     # K -- во сколько раз больше нормы считать тревогой
TRADING_DAY_MINUTES = 390                          # длина торговой сессии США в минутах (для масштабирования √t)

# ─── ПРОТУХАНИЕ ПРИКАЗОВ И АЛЕРТОВ (см. Claude/BACKLOG.md, п.5, 2026-07-30) ───
# Ось 1 (ушла цена, БЕЗ времени -- пересчитывается заново на каждом цикле котировок):
# is_stale_price = |текущая_цена - цена_приказа| / цена_приказа > daily_volatility_pct × K'
STALE_PRICE_VOLATILITY_MULTIPLIER = 3.0            # K'
# Ось 2 (прошло время, независимо от цены):
STALE_ORDER_TIME_LIMIT_DAYS = 30

# Консервативная (вход, буферная зона дисконта, Claude/16_selection_logic_audit.md,
# находка Q, 2026-08-08): порог "цена ниже SMA200" -- фиксированный 0.00% отсекал
# качественных лидеров, которые торгуются буквально у своей SMA200 (в пределах
# собственного дневного шума), не давая им статистически значимой просадки.
# signal_price_to_sma200_pct < K × signal_daily_volatility_pct -- та же нормализация
# по волатильности бумаги, что и выше, K подобран вручную под сегодняшние данные.
CONSERVATIVE_DISCOUNT_BUFFER_MULTIPLIER = 2.5      # K

# ─── ПОДТВЕРЖДЁННЫЙ СЛОМ ТРЕНДА (см. Claude/BACKLOG.md, 2026-08-02) ───
# tickers.signal_ema20_streak_days -- знаковая длина серии: сколько торговых дней
# подряд цена закрытия по одну сторону от EMA20 (+N выше / -N ниже), считается раз
# в сутки в sync_signals_yf.py из уже загруженной истории цен. "Подтверждённый слом"
# = смена знака серии, устойчиво держащаяся >= порога ниже -- фильтр однодневного шума
# (см. живой пример VRTX/BA против MU/META, Claude/12_investment_goal_and_mechanisms_roadmap.md).
# Фиксированное число дней, не нормализуется по волатильности бумаги -- ось "как долго
# держится состояние", а не "насколько большое движение" (для последней уже есть
# отдельная нормализация выше). Один порог, три места применения с разной полярностью:
# выход Трендовой (streak <= -N), вход Консервативной шаг 2/3 (streak >= +N),
# "сдаться" по зависшей ступени 1 (streak всё ещё < +N дольше tactic_give_up_days).
TREND_REVERSAL_CONFIRM_DAYS = 3

# ─── ЕЖЕМЕСЯЧНЫЙ ПЕРЕСМОТР СЛОТОВ Р/Т (Claude/13_portfolio_construction_and_rebalancing_rules.md, 2026-08-03) ───
# Фиксированные суммы слота (tactic_slot_fixed_usd) Револьверной/Трендовой сознательно
# не масштабируются с капиталом (число одновременно доступных качественных идей не
# растёт вместе с богатством семьи) -- но раз в месяц дайджест напоминает факты для
# СОЗНАТЕЛЬНОГО решения (поднять сумму или продолжать копить число слотов), не меняет
# rules_config сам. day-of-month, не отдельный cron-джоб -- переиспользует то, что
# assemble_portfolio_digest_data и так вызывается не чаще раза в сутки (digest_clock_loop).
MONTHLY_SLOT_REVIEW_DAY = 1

# ─── СЕКТОРАЛЬНАЯ ДИВЕРСИФИКАЦИЯ ПОРТФЕЛЯ (Claude/BACKLOG.md №82, 2026-08-05) ───
# Заводское значение portfolios.sector_target_config (jsonb) для НОВОГО портфеля --
# потолок доли каждого сектора от total_capital портфеля (не цель, срабатывание
# одностороннее -- только превышение). Ориентир -- примерные веса секторов широкого
# рыночного индекса (S&P 500, таксономия секторов как у Yahoo/tickers.sector), чтобы
# заводское значение не было произвольным equal-weight. Пользователь может
# скорректировать под конкретный портфель после создания -- это только стартовая точка.
DEFAULT_SECTOR_TARGET_CONFIG = {
    "Technology": 32.0,
    "Financial Services": 14.0,
    "Healthcare": 10.0,
    "Consumer Cyclical": 10.0,
    "Communication Services": 9.0,
    "Industrials": 8.0,
    "Consumer Defensive": 6.0,
    "Energy": 3.0,
    "Utilities": 2.5,
    "Real Estate": 2.0,
    "Basic Materials": 2.0,
}

# ─── МОМЕНТ ИСПОЛНЕНИЯ СДЕЛКИ: MARKET vs LIMIT (сигнал A, Claude/19_price_move_protection_design.md, 2026-08-13) ───
# Не "когда" исполнять (система для настоящих портфелей никогда не исполняет сама), а "по
# какой цене" рекомендовать -- рынок или лимит. Два независимых режима по marketState
# (yfinance): REGULAR (+ прошёл разгонный буфер) -- VWAP сессии; иначе -- статистический,
# та же логика PriceMoveWatcher (движение за короткое окно, но БЕЗ уведомления человеку).
EXECUTION_TIMING_VOLATILITY_MULTIPLIER = 2.0       # K -- рабочая гипотеза, не откалиброван бэктестом (в отличие от K_стоп/K_трейлинг)
EXECUTION_TIMING_SESSION_WARMUP_MINUTES = 15       # минут с открытия сессии, прежде чем доверять VWAP
EXECUTION_TIMING_LOOKBACK_MINUTES = 45             # окно для статистического режима -- то же, что PRICE_MOVE_WATCHER_WINDOW_MINUTES

# ─── ЗАЩИТА КАПИТАЛА: СТОП-ЛОСС / ТРЕЙЛИНГ-СТОП (Claude/19_price_move_protection_design.md, 2026-08-11) ───
# Сами K-множители (tactic_stop_loss_k / tactic_trailing_stop_k) живут в
# strategies.rules_config (per-стратегия, как и остальные tactic_*, откалиброваны
# бэктестом на реальных данных -- см. дизайн-документ), не здесь. Здесь -- только
# системная механика повтора, симметрично PRICE_MOVE_WATCHER_PERIODIC_SEC выше.
CAPITAL_PROTECTION_PERIODIC_SEC = 3600      # повтор уведомления, пока условие не снято
